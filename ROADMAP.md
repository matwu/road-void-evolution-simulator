# ロードマップ

## このプロジェクトで実現したいこと

車載型コンパクトGPR（地中レーダー）向けに、**道路下の空洞（void）が時間とともに成長し浮き上がっていく様子**を検知・追跡できるAIモデルを作るための、**gprMaxベースの学習データ生成基盤**を作ること。

実データ（実際の道路陥没事例）は数が少なく収集も困難なため、電磁波シミュレーションで「空洞の成長パターン」を大量かつ多様に再現し、機械学習の学習/評価データとして使えるCSV・B-scan画像を出力できるようにすることが最終目的。

## 現状（2026-08-23時点）

コミット4件（`first commit` → `simulation config改善` → `ratio化` → `bscanプロット改善`）で、**データ生成パイプラインの土台**ができている段階。

- ✅ Docker実行環境（gprMax同梱）
- ✅ `simulation_config.yaml` によるパラメータ管理（道路層構成・GPR設定・空洞パラメータを比率ベースで指定可能に改善済み）
- ✅ 空洞の時間発展モデル（成長率・上昇率をシーケンス内のstageごとに反映）
- ✅ gprMax実行ラッパー（`run_gprmax.py`、並列ワーカー対応）
- ✅ HDF5 → CSV変換（単体/バッチ/シーケンス結合の3モード）
- ✅ 可視化（A-scan / B-scan / 全成分プロット）

一方で、以下はまだ手つかず：

- ❌ 生成データの検証・品質チェック（物理的妥当性、双曲線反射パターンの確認など）
- ❌ 自動テスト（ratio計算やCSV/HDF5整合性の単体テスト）
- ❌ 大規模データセットの実生成・実行時間/メモリ特性の検証
- ❌ 機械学習側（ラベリング、学習用データセット形式への変換、ベースラインモデル）
- ❌ 実データとのsim-to-real比較

## フェーズ別ロードマップ

### Phase 1: データ品質の検証（次にやるべきこと）
- 生成した`.in`/`.out`が物理的に妥当か検証（誘電率コントラストによる反射強度、双曲線パターンの出現）
- `gpr_simulator.py`のratio計算・境界値（空洞がドメイン外にはみ出さないか等）に対する単体テスト追加
- `export_to_csv.py`の出力がHDF5の生データと一致することを確認するテスト
- `config/simulation_config.yaml`の妥当なパラメータ範囲をドキュメント化

### Phase 2: データセットの多様化・スケール
- シナリオの多様化: 複数空洞、土壌水分・材質バリエーション、道路層構成違い
- アンテナ特性を模したノイズ・クラッタ（表面反射、リンギング）の付加でリアリズム向上
- `num_sequences`を増やした大規模生成のパイプライン化（並列実行の最適化、進捗管理）
- 生成データセットのバージョニング/manifest管理（再現性の担保）

### Phase 3: 機械学習への接続
- 空洞のground truth（位置・サイズ・成長段階）とB-scan/CSVを紐付けるラベリング仕様の設計
- 学習用データ形式（npz等）へのエクスポート機能
- 検知・追跡タスクのベースラインモデル（例: B-scan画像に対するCNN）で生成データの有用性を検証
- train/val/testスプリットツール

### Phase 4: 自動化・スケールアウト
- CLAUDE.mdに記載の「自動品質チェック」の実装（生成後に異常値・破損ファイルを自動検出）
- 並列処理のさらなる最適化（マルチノード対応など）
- シミュレーション中のリアルタイム可視化

### Phase 5: 実データとの橋渡し（sim-to-real）
- 実際の車載GPRデータ（入手できれば）とシミュレーションデータの統計的比較
- ドメインランダマイゼーション等でsim-to-realギャップを縮小

## 優先度の考え方

現状はPhase 1が空白（検証・テストが皆無）なのがリスク。データ生成機能は一通り揃っているが、「出力が正しいか」を確認する手段がないまま量産フェーズ（Phase 2）に進むと、誤った物理設定のまま大量データを生成してしまう恐れがある。次の一手はPhase 1（検証・テスト整備）を推奨。
